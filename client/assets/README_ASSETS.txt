Both real assets are in place:

- profile.jpg   → your uploaded photo, used in the hero section
- resume.pdf    → your uploaded CV, linked from the "Download CV" buttons

The old avatar-placeholder.svg is unused now — safe to delete, or keep as a
fallback if you ever want to swap photos without breaking the layout.

To update either file later, just replace it in this folder, keeping the
same filename (profile.jpg / resume.pdf) — no HTML changes needed.
